# Publication attempts

The initial draft-release request used abbreviated target `f939782` and GitHub
rejected it with HTTP 422 (`Release.target_commitish is invalid`). No release or
asset was published by that request. The retry uses the full pushed commit ID
`f939782b341610688b788a5d50c9e00a4c79f4d0`.

The prior `windows-demo-2026-09-22` release is retained unchanged. Expanded graphics
use a separate `graphics-demo-2026-09-22` prerelease.
