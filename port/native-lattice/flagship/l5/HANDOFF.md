# L5 lobby/witness handoff — incomplete, pre-engine only

Source lock: `515daf07589150dd3241f4ae1425cc1b093912f5`; base HEAD `d23d02a56ba622defffc94c249a08af9b32350c6`. This lane was delivered without engine/server work. L1–L4 working-tree changes were preserved. No source, server, source lock, base network client/session, or lead-owned aggregate verifier was edited.

## Delivered

- `godot/lattice/board.gd`: parses launcher options through the L1 `SessionOptions`, uses `SessionFlow` host configuration and authoritative roster, waits for explicit host Start/restart after source echo, keeps guests waiting without the 15-second roster timeout, retains bounded connect/config/start timeouts, displays requested versus echoed values and final source result, sends loadout in one room create/join, and resets purchase consent. Historical `--smoke` remains an explicit verification-only auto-start path. Unconfigured/invalid CLI options are blocked rather than silently falling back. Board has no source-gameplay rule edits.
- `port/tools/native_lattice_flagship/audit.mjs` and `audit.test.mjs`: source/recipient/native/cleanup gates with ten *synthetic* audit mutations (no owner flip, wrong participant/revision, ACK-only result, no cleanup, wrong event, bot-only rung, incomplete wave, missing restart). Source `cocs-capture.participants` is attribution evidence only when it matches the local peer's recipient-published post-assignment actor ID; order settlement is not capture.
- `run.mjs`: marker-gated 20-minute single-client owned loopback attempt; 12,000-row/16-MiB wire ceiling, 10,000-line native trace source bound, private XDG, result/cleanup logs, input/hash/pin manifest, report remains BLOCKED unless actual native UI review, genuine outcome, attribution, and restart evidence are supplied. It does not fabricate movement, results, native UI review or human identities; an unattended client cannot PASS. Source start revision added to native trace as an explicitly *derived* witness correlation. No independent two-native-client orchestration is claimed.
- `rung_floor.mjs`: marker-gated ordinary server socket-seat experiment on both maps/rungs for 1, 2, 7, 8 and an 8→7 pre-start disconnect case, retaining ordinary response, config and floor echo. Start echo may not include filled population, so the script does not assert a fabricated bot-count echo. Socket seats are automated, **never** humans; not run here. Source grace/disconnect behavior might make the disconnect case fail; retain that failure instead of coercing a PASS.
- `rung_sim.mjs` / `rung_sim.test.mjs`: privileged seeded source `Match` experiment across both authored maps and both rungs. Runs the dominance ratchet/reset/contest and time tie-break via source APIs, and checks role/floor/fill through source configuration APIs. Every attempt writes unique observed values/failures with `evidence_class: direct-sim`; it is not ordinary-seat evidence.
- `godot/tests/lattice/flagship_l5_contract.mjs`: static integration seam check, not a GDScript parse or live UI test.

## Checks and evidence class

The first delivery passed 10 synthetic audit tests and four direct configuration cases (`port/native-lattice/evidence/flagship/direct-sim-1790315827703/results.json`). Subsequent repairs added a 13-test audit, two-client gate fixture, and 12-case privileged direct Match matrix. The integrated pre-engine aggregate passed 187 Node tests and the nine-map semantic export at `port/native-lattice/evidence/flagship/contracts-1790316494062383366/`. These are contracts and direct experiments, not native or natural acceptance. No Godot parse/import or server-seat run was attempted before the marker.

## Follow-up / unverified seams

1. Lead must schedule Godot import, native Board setup/host/guest pixel and input checks, verify actual source frame echoes, retry/restart, and native pointer/command safety. `SessionFlow.start` itself checks the source floor echo but cannot prove a human identity; the host may still receive a source below-minimum rejection. Board's current `--smoke` is a short verification path, not a natural round.
2. Lead must run/extend `rung_floor.mjs` under an authorized server slot, then implement the missing *full* direct source Match matrix (dominance 3/4, idle/no decay, remote order, adjacency vs supply, deadline tie-break), with privileged-state access labelled direct-sim. Existing narrow config experiment is not MVP-B.
3. `run.mjs` remains a single-client witness scaffold; `two_client.mjs` adds an independent human-assisted host/guest runner, but it has **not** been run. No G6/C1 claim follows from its static fixture. Actual OS/engine/human input provenance and reviewed screenshots remain live gates. To audit manually added UI evidence, append a versioned `native.jsonl` UI observation with reviewer/screenshot and explicit source revision; never generate one from server frames.
4. Audit requires a capture with a participant matching a visible actor for a positive-capture claim, while natural-defeat is separate and compares the winner to the recipient-published local team. Both require genuine results, native evidence and restart; Operations success requires fifth-wave clear. The `run.mjs` elapsed bound is 20 minutes for **one** attempt; restart must occur before it expires.
5. Human C2 requires eight actual people and retained roster/playtest answers. Socket floor results are not C2 and no roster field certifies human presence.

## Repair status (2026-09-25)

This handoff remains **incomplete**. Audit UI evidence now requires reviewer and screenshot provenance plus matching local actor and source round. Audit output separates `positive_capture` from `natural_defeat`; a legitimate source defeat is no longer made contingent on an ownership flip/capture. These are audit-contract changes only, not live evidence.

Explicit remaining gaps (not PASS):

- `run.mjs` still launches one native process. It does not yet orchestrate independently attributed host and guest peers, wait for ordinary guest lobby assignment before host start, or prove guest cannot configure/start. No two-client behavior is claimed.
- `rung_sim.mjs` now executes a short seeded direct `Match` on both maps and both rungs. Explicit unavailable gaps: adjacency-versus-supply capture (requires natural actor capture through `Match.step`; privileged owner assignment is not capture; refs: `game/cocs.mjs` `capturableBy` and `stepCocs`, `game/core.mjs` `Match.step`); remote order/idle no-decay (order APIs command tasks rather than node ownership/decay; refs: `game/cocs.mjs` `cocsCommandAction` and `stepCocs`). These are not claimed or synthesized.
- The UI provenance format has contract coverage only; no screenshot/reviewer evidence exists. The existing synthetic complete audit fixture is not a live acceptance result.
- No engine marker was consumed. Do not start Godot/import, full rounds, server/socket-seat tests, packages, or a multi-client runtime until authorized.

Focused audit tests and source API discovery are the only repair verification; prior config-only sim results remain historical evidence and do not satisfy G5/G6. Status: MVP-A **PENDING**, MVP-B **PENDING**, MVP-C1 **PENDING**, MVP-C2 **PENDING**.

Status at first repair: MVP-A **PENDING**, MVP-B **PENDING**, MVP-C1 **PENDING**, MVP-C2 **PENDING**. The later two-client command is documented below, but has not been run.

## Direct Match experiment repair (2026-09-25)

`node port/tools/native_lattice_flagship/rung_sim.mjs` runs 12 per-map/rung case records with seeded `Match` construction (`game/core.mjs`), short `Match.step` ticks, published `cocsSnapshot` dominance (`game/cocs.mjs`), `cocsOutcome` deadline resolution, and `cocsRungPlan`/`cocsRoleAllowed` gates (`game/config.mjs`). Permitted privileged ownership/score/time setup is labelled `direct-sim`; no direct mutation is presented as natural capture. Each run emits a unique `direct-match-<timestamp>-<pid>` directory containing `results.json`; assertion failures are recorded per case and produce exit status 1. Run `node --test port/tools/native_lattice_flagship/rung_sim.test.mjs` for the focused check. This does not close the exact gaps above, nor authorize server, full-round, or multi-client work.

## Two-client executable witness (2026-09-25)

Added `port/tools/native_lattice_flagship/two_client.mjs` with focused `two_client.test.mjs`. It refuses server/Godot work unless the engine-slot marker and `GODOT_BIN` exist, pins and checks the source Godot version, creates exactly one owned loopback game server, launches independent host and guest native processes with `--native-trace`, and has the guest use the host welcome's room ID with `--join-room`. The gate is based on recipient-published guest lobby presence plus at least two distinct actor IDs. Once ready, the operator must genuinely click the host's native **Start / restart (host)** control. Terminal Enter only acknowledges the click; it sends no game protocol message. The script waits for the host socket's actual source `start` frame. There is no safe UI-driving facility in this scope, so automatic start is deliberately not claimed.

Sol integration correction: pre-start ordinary roster actor IDs are null. The gate now waits for two distinct connected ordinary **peer IDs** and the host configuration echo, then for both recipient starts and distinct post-start actor assignments. World Practice/Operations now wait for an explicit host Start instead of auto-starting before a guest can join. The guest command line omits host-only `--time-limit`; the native parser and both launchers reject explicitly supplied host fields on guest joins. The audit's natural-defeat claim compares the winner against the recipient-published local team, including a team-1 fixture. These corrections passed focused Node tests; native behavior remains unverified until the slot.

Artifacts include per-socket recipient and outgoing frames, per-client native trace/log streams, results, bounded capture (12,000 rows / 16 MiB), and a hash/pin evidence manifest. Both detached process groups and the owned server are cleaned up. Guest inability to start is **UNVERIFIED**; outgoing-frame recording is observational, not a security proof. On timeout or missing genuine click/source start the run is BLOCKED, not passed. Run only after marker authorization: `GODOT_BIN=... node port/tools/native_lattice_flagship/two_client.mjs --map=asterion-relay --mode=cocs`. No live run was performed here. Focused verification: `node --test port/tools/native_lattice_flagship/two_client.test.mjs` (synthetic state/cleanup-independent gate only); `node --check` likewise does not exercise Godot/server.
