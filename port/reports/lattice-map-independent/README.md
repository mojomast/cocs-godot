# Independent native LATTICE map acceptance

Runtime `c982d25`, attributable integration of `596325d`. The lead reviewed both
the coordinate-diagram renderer and board selection lifecycle, then ran:

```sh
PORT=0 TMPDIR=/tmp/opencode \
GODOT_BIN=/home/mojo/.hermes-instances/fresh/workspace/godot-toolchain/Godot_v4.5.2-stable_linux.x86_64 \
python3 -B port/native-lattice-map/verify.py
```

**PASS**: all eleven steps in
[`1790042036336949082/results.json`](../../native-lattice-map/evidence/1790042036336949082/results.json).
Source/semantic export, import, original adapter38/UI10 and new map23 checks pass.

Actual graphical `board.tscn`, native engine mouse/key dispatch, normal-rate
source authority, independently owned loopback servers and private displays:

| Case | Map input checks | Original List checks |
|---|---:|---:|
| Asterion PvP, 960×640 | 26 | 28 |
| Monsoon PvP, 1280×800 | 26 | 28 |
| Asterion co-op, 960×640 | 26 | 20 |

Each Map case verifies native List/Map switching, marker click and keyboard
selection, synchronized list state, one explicit HOLD to the clicked public
frontier, matching source card/actor/peer/round/target acceptance and running
state, no automatic purchases and disconnect cleanup. Original List flows still
confirm exactly one PvP Fighter and 12 FLUX spent. All six cleanup reports show
the native process exited, HTTP server closed and temporary runtime removed.

The lead directly opened all three Map receipt images and the 960×640 Map
disconnect image. Team symbols, authored labels, selection ring and receipt are
visible at both resolutions. After disconnect, markers/selection are cleared;
the unknown-resource text wraps and the existing outer scroll contains the
longer disabled view. Screenshots have been copied to the current gallery.

List remains the default. This is a public-objective coordinate diagram with
independently fitted X/Z axes, no inferred topology, enemy actors or terrain
distance promise. Native engine input is not OS-device automation; accepted
HOLD is not capture/completion. Original failed/superseded attempts remain
preserved in the agent evidence. The new map23 regression is registered in the
combined verifier; the subsequent complete **52-gate run passes** at `c982d25`
plus the map-gate change. Exact results are in `../verification.json`.
