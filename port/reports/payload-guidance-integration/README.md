# Lead integration of Payload approach guidance

Delivery `20f85f2` is integrated. Runtime changes are limited to objective
guidance/HUD/renderer; authoritative source rules, input and shared session remain
unchanged. The lead reviewed snapshot geometry/role/status handling and ran:

- Pinned Godot import: PASS.
- Guidance geometry/snapshot/unknown-state contracts: **823 PASS**.
- Real HUD layout/state fixture: **99 PASS**.
- Historical full-Payload sequence through the **current integrated renderer**:
  **3,793 models / 3,792 matching snapshots**,148 rollback and79 bank samples.

Exact commands and results are in `checks.json` and logs. The replay's source
delivery124.117s, input receipts, result and restart are historical. Its original
`nativeCompletionProven:false` remains intact; this check establishes renderer
compatibility, not new live objective completion.

The lead directly opened delivered final off-cart960×640 and escort1280×800 PNGs.
Cart Right/19.7m/Get within4.5m/Controls released fits the gold row at small size;
the escort view shows Inside4.5m and source PUSHING RED separately. The source
radius, planar distance and source route progress are not conflated with proof
that this local actor caused the push. Close-cart clipping and world captions
remain inherited visual issues.

Original zero-height-label visual failure is preserved. Later bank/defender/dead/
malformed-field refinements have fixture/replay coverage, not fresh live images.
Two new aggregate gates are registered; complete aggregate and final package
verification follow the pending spectator runtime repair.
