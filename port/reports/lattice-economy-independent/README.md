# Independent co-op recruitment acceptance

Scoped delivery `56a9abb` integrated at `642c615`. The lead reviewed the ordinary
REINFORCE frame, recipient projection, executor/slice/window/thread gates,
fresh consent and pending-action deduplication before running:

```sh
PORT=0 TMPDIR=/tmp/opencode \
GODOT_BIN=/home/mojo/.hermes-instances/fresh/workspace/godot-toolchain/Godot_v4.5.2-stable_linux.x86_64 \
python3 -B port/native-lattice-economy/verify.py all
```

**PASS**, all 15 steps in
[`1790043604418131510/results.json`](../../native-lattice-economy/evidence/1790043604418131510/results.json).
This includes locked export/import, selected source60 (one existing skip),
adapter38/UI10/map23/economy91, two actual co-op purchase sessions and all six
original Map/List graphical cases. Sessions use separate owned normal-rate
source servers and private displays. Native input is engine mouse/key dispatch.

Both co-op cases at **960×640** click Connect, select Map/frontier, explicitly
issue HOLD, wait for the natural between-wave window, authorize a fresh purchase,
Tab/Enter REINFORCE, suppress repeated clicks and observe source confirmation:

| Case | Cumulative FLUX spent | Spawned counter | REQ spent | Native / wire checks |
|---|---|---|---|---|
| Asterion | 35 → 85 | 0 → 1 | 0 → 0 | 26 / 9 |
| Monsoon | 28 → 78 | 0 → 1 | 0 → 0 | 26 / 9 |

There is exactly one incoming HOLD and one REINFORCE per run. The `done/ok`
card matches native peer/actor/round/action identity. Price proof uses cumulative
spending, not wallet subtraction while income changes. Public own-actor REQ is
displayed; enemy wallets remain absent. Strict per-action unit attribution in
concurrent multiplayer races remains limited by the source receipt schema.

The natural wait was about two minutes, with no clock override or injected
budget. Initial HOLD was accepted/running, then expired normally with `ttl`;
the native receipt explains “Order expired; issue HOLD again.” No capture or
HOLD completion is inferred from acceptance or later blue node ownership.

The lead directly opened both final receipt PNGs, Monsoon's waiting-window PNG
and Asterion's disconnect PNG. Prices, explicit authorization, role label,
friendly rejection and confirmed receipt fit at 960×640. Disconnect clears
map selection/targets and disables actions; longer unknown text/footer uses
the existing outer scroll. All per-case cleanup records confirm native exit,
HTTP closure and temporary runtime removal. Six original Map/List cases still
pass, including PvP's exactly-one Fighter at 12 FLUX.

The current combined **59-gate suite passes**, including the new economy91 gate.
Native world traversal is a separate active slice; human/OS-device acceptance,
full co-op campaign and native recording completion remain open.
