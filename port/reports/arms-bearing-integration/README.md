# Lead integration: Arms Race and sports bearing correction

Arms Race delivery `52de3b9`/`21fbd93` integrated as `2335279`/`99e4413`;
independent review `a7cbc12` as `879342e`. Sports bearing correction
`99c0a93`/`6350cc4` integrated as `0b820c9`/`e1defc0`.

The lead connected Arms Race to both launchers, setup availability, package help
and exported-scene inventory. All three combat arenas route to `armsrace`;
invalid sports maps, wrong modes and unsupported timer options fail before
authority startup. Default runtime remains two Normal bots/180 seconds/ten rungs.

The lead reran the independent Arms Race offline audit on a temporary evidence
copy, retaining its output here. One independent combat attempt, one failed
graphical preflight and one successful lifecycle/startup batch remain distinct.
The audit confirms source kill/promotion/display and ordinary timed results,
not a completed ten-rung victory. Original evidence was not rewritten.

Direct lead visual review:
- Independent Arms Race promotion1280×800: rung2 Rocket Launcher, next Rail Lance,
  frags1 and ammo/weapon panel agree and are legible.
- Independent Arms Race results960×640: all three ladder/frags/deaths rows and
  restart guidance fit. Grok's timed ranking is not a local ladder finish.
- Ion1280×800 `00-spawn`/`02-turn`: actual gate right/left matches corrected labels.
- Aurora960×640 goal-run `00b-right`/`02-turn`: ball and opponent goal right/left
  match labels. The distant ATTACK caption remains partly behind the HUD.

Camera bearing regression uses Camera3D projection as its oracle (896 cases).
Two old soccer-test lateral expectations were incorrect and corrected; source
steering, gameplay observers, Ahead/Behind thresholds and physics are unchanged.
Inherited gameplay victory evidence remains historical rather than rerun here.

Three new aggregate gates cover Arms Race source rules, the combined55 native
fixtures and sports bearing projection. All **73 aggregate gates PASS**. Fresh
eight-scene Linux build/startup evidence passes in
`../linux-arms-bearing-independent/`; final metadata-complete archive preserves
all111 successfully exercised runtime files byte-for-byte.
