# Bounded live attempt ledger

1. **Payload, first and only live attempt — PASS.** `2d6b7e66-6adc-43f2-b29e-9d26fed86387`, ordinary 180-second limit, 235-second outer bound, 960×640. Full checkpoint-1 rollback to its bank and delivery at 124.117 seconds; actual attacker results/restart. Runtime source at commit `460674e`; original summary/validation/images/logs retained.
2. **CTF, single allowed live attempt — original strict acceptance FAILED; source pass/capture completed.** `cfb8c298-c0e0-4337-9951-75b7bcc734a3`, ordinary 60-second limit, 110-second outer bound, 1280×800. Source pickup actor 0 at 19.483, genuine `flag-pass {actor:0,to:2}` at 20.867, recipient capture at 38.367. No source flag drop or second pickup. Actual 60-second Red result and restart/fresh capture. Original gate failed solely at the immediate physical-key-release assertion: after queuing ordinary key-up events, `controls_released()` was still false in the same callback. Mouse capture was released and control eligibility false. The live driver's E release was stage-dependent, so an early successful pass also left E held until results neutralization. The source rising edge prevents repeated passes, but this is a test-driver pulse defect.

Pinned Godot synthetic follow-up `completion_inputs.gd` reproduces the witness timing exactly: physical E is **still held immediately after parsing its key-up**, but **released after two process frames** (`input-timing.log`). This is an observer-timing distinction, not evidence of a shared session defect. The original CTF summary remains failed. No rerun, seed selection, authority write or clock change was used. Any narrower gameplay replay must state exactly which result/input facts are actually present in the original archive rather than retroactively claim a settled result-frame key observation.

Both live attempts cleaned up all owned native/display PIDs, loopback server, sockets and temporary runtime trees. There were no route collisions or bounded navigation failures. The failed offline route-import command and successful corrected discovery command are recorded in `DISCOVERY.md`.
