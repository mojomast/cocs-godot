# Attempt history

All attempts use their own evidence UUID, owned display/loopback service, normal source timing, and scoped cleanup. Failed evidence stays failed in its original `summary.json`.

1. `dc191762-a9b2-4625-9d36-575a32c16323` — CTF workflow itself observed source pickup (19.967s), drop (20.067s), defender return (20.633s), second pickup (21.683s), capture (40.9s), actual 90-second results/restart. Overall **failed**: async screenshot/quit delay let `_process` print three completion markers. Fixed with a one-shot finish guard. Direct PNG review found the hint row lacked visible layout height; corrected the label's wrap/trimming/minimum-height settings. Results status duplication was removed in favor of the shared scoreboard footer.
2. `931c3c22-04c2-43b8-88b2-2b6f0e55e98e` — Payload **failed** to reach cart/contest. Primary stopped at x≈-78,z=-0.162; defender at x=78,z=-7.172. These are authored collidable arch posts, not source input failures. Read the gateway geometry and added ordinary walking detours along x=-82/+82. Actual source time-limit results/restart still occurred, but objective progression was not claimed.
3. `80541274-37eb-4041-8c02-07caf748a230` — CTF **passed**: 1,870 correlated snapshots; return, capture, 60-second result/restart and fresh capture. Direct PNG review checked capture, results and restart at 1280×800. Later final run improves capture framing by walking away from the home flag pole and looking back through native mouse input.
4. `d83c48ad-725d-46ba-bc1f-119d40542ce0` — Payload observed contest, resumed push, checkpoint 1, idle, actual 90-second results and restart. Overall **failed** due to an overly strict first-restart-snapshot distance assertion (`0.05 !== 0`): source legitimately pushed for two ticks after a normal spawn inside cart radius. Corrected validator bounds fresh distance by source speed × fresh elapsed time and still requires zero checkpoint/score and cleared old renderer. Direct PNG review checked contest/checkpoint/results at 960×640. Final observer follows at 3.2m (inside 4.5m escort radius) to frame the cart rather than overlap its near-eye mesh.
5. `c45137f0-884e-433e-9d22-01f6be647ce6` — final CTF **passed**: 1,868 correlated snapshots; return, capture, genuine 60-second results/restart. Reviewed actual capture/results/restart PNGs at 1280×800. The results view includes the inherited large WEST CITADEL world landmark behind the panel; the objective flag label itself is smaller and the objective/results text remains readable.
6. `7323f104-2d53-4a65-a536-a4c482b82104` — Payload source workflow again completed; initial gate **failed** because its new speed×time bound ignored independent three-decimal wire rounding (`time=0.033`, `distance=0.05`). Read `game/quantize.mjs` and added the exact half-quantum uncertainty for time, speed and distance. Current-validator replay passes all 2,769 correlated frames with 80 contested snapshots, resumed push, checkpoint/idle, and results/restart. Original failed summary is untouched. Direct PNG review confirmed the cart is now visible from the ordinary escort position and the 960×640 panel HUD remains readable.
7. `38216605-573c-47cf-a23a-18499282579c` — final Payload **passed**: 2,769 correlated snapshots, 80 contested snapshots, resumed push, checkpoint 1, stable idle, 90-second source results and restart/fresh capture. Direct `read` inspection of actual contest/checkpoint/results/restart PNGs at 960×640 confirms panel readability, cart framing and reset display. Replay verifies packed-log hashes and passes; both owned PIDs are reaped/absent, zero sockets, owned server closed and temporary runtime tree removed.

Synthetic development regression: initial new HUD fixture expected integer score formatting while Godot JSON numbers rendered as `2.0 : 7.0`. Fixed display formatting; original observer string API still carries the same authoritative score/status. No source change.
